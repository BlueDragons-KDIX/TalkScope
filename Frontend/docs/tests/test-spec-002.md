# テスト仕様書 002 — デモ重要語・セッションリセット・モック語検出

作成日: 2026-05-02  
テストランナー: Bun Test  
補足: `test-spec-001.md` の続報（追加・変更分を記録）

---

## 結果サマリー（本書作成時点）

| 項目 | 結果 |
|------|------|
| 総テスト数 | 48 |
| 合格 | 48 |
| 失敗 | 0 |
| テストファイル数 | 11 |

---

## 追加・更新されたテスト

### 1. stores — termStore.test.ts（追記）

**目的**: グローバルリセットとデモ用語除去の振る舞いを固定する。

| # | テスト名 | 期待結果 | 結果 |
|---|---------|---------|------|
| 1 | `resetSession` で用語・選択・履歴・ピン・重みを初期化できる | 各フィールドが初期状態に戻る | ✅ |
| 2 | `stripDemoImportantTerms` は `demo-important-` の用語だけ除去する | そのプレフィックス以外の用語は残る | ✅ |

### 2. debug/demo — mockImportantTerms.test.ts（新規）

**目的**: サーバー代替のモック重要語検出がデモ文と整合することを確認する。

| # | テスト名 | 期待結果 | 結果 |
|---|---------|---------|------|
| 1 | 空文字では何も返さない | 長さ 0 | ✅ |
| 2 | 即時デモ文から複数のモック語を検出する | 代表 ID が含まれる | ✅ |
| 3 | 語が無ければリストに含めない | マッチなしで空 | ✅ |
| 4 | `MOCK_IMPORTANT_TERMS` はすべて `demo-important-` ID を持つ | 命名規約の一貫性 | ✅ |

---

## 自動テスト対象外（変更なし）

`test-spec-001.md` の「カバレッジ対象外」に従う。  
グローバルツールバー・ポップアップ・モーダルは手動で確認する。

---

## 手動テスト確認項目（追記）

以下を `bun run dev` で確認する。

- [ ] ツールバー「テスト」からデモテキスト読込・ライブデモ・デモ重要単語マーキング ON/OFF が動く
- [ ] マーキング ON 時、モック語が文字起こしにハイライトされバブルに現れる
- [ ] 「表示」からダークモード・アクセントが切り替わる
- [ ] 「リセット」で文字起こし・用語・履歴・詳細選択・バブルが初期化される（レイアウトは維持）
- [ ] 文字起こしウィンドウのフッターにリセットが無い（プレゼンレイヤ）

---

## 追加（2026-05-02 追記）— 補色テーマ

### 3. presentation/utils — oppositeThemeColor.test.ts（新規）

**目的**: 発表後フェーズで使う `getOppositeThemeColor` のマッピングを固定する。

| # | テスト名 | 期待結果 | 結果 |
|---|---------|---------|------|
| 1 | blue と orange が双対 | 相互に正しく変換 | ✅ |
| 2 | rose と emerald が双対 | 相互に正しく変換 | ✅ |
| 3 | indigo は orange、purple は emerald | パレット上の割当 | ✅ |
| 4 | マップにないキーはそのまま返す | 未知キーは変更しない | ✅ |

テストランナー実行時の総数は `bun test` の出力に従う（本追記で +4 ケース）。

---

## 追加（2026-05-05 追記）— 重要度ランキングウィンドウ

### 4. presentation/utils — importanceRanking.test.ts（新規）

**目的**: 重要度ランキング用の合成スコアと表示件数計算を固定する。

| # | テスト名 | 期待結果 | 結果 |
|---|---------|---------|------|
| 1 | 重要度スコア降順でソートする | score の降順で並ぶ | ✅ |
| 2 | シグナル未定義は 0 扱い | frequency/clickWeight が 0 | ✅ |
| 3 | ウィンドウ高さから表示可能件数を算出 | 最低 1 件以上を返す | ✅ |

### パフォーマンス・Go/No-Go 判定メモ

- 実装方針:
  - `ImportanceRankingWindow` は `transcript` の生値ではなく `useThrottledValue`（160ms）を使って再計算頻度を制御
  - 表示は `visibleCount` まで `slice` し、過剰描画を防止
- 自動検証:
  - `bun test src/presentation/utils/__tests__/importanceRanking.test.ts` 通過
  - `bun run build` 通過
- 判定:
  - 現時点では **Go（継続）**。既存機能へ直接高頻度再描画を追加しない設計で導入済み
  - 手動で録音中の体感遅延が確認された場合は、`importanceRanking` ウィンドウをレイアウトから外して即時無効化する

---

## 追加（2026-05-05 追記）— ランキング UI/UX 改善

### 5. presentation/windows — ImportanceRankingWindow（操作性改善）

**目的**: 重要度ランキングの視認性と操作導線を改善し、閲覧とピン操作を分離する。

- ヘッダー左側: `重要度順 / スターのみ` の条件選択を配置
- ヘッダー中央〜右: 要素サイズを連続調整できるスライダーを追加
- 行操作:
  - 左クリックは詳細確認（順位変動なし）
  - 右クリックでスターON/OFF
  - マウスホイールで上下スクロール
- 視認性:
  - 単語文字サイズの増加率を拡大
  - 順位数字を強調バッジ表示へ変更

### 手動確認チェック（追記）

- [ ] 条件選択を `重要度順` と `スターのみ` で切り替えできる
- [ ] 要素サイズスライダーで行高さと単語サイズが連動して変わる
- [ ] 行の右クリックでスターが切り替わる
- [ ] ホイールスクロールでランキングを上下できる

---

## 関連ドキュメント

- **`test-spec-003.md`**（2026-05-14）: PR #26〜#29（アクセント・文字起こしモード同期・TermDetailPanel・録音UX／ウィンドウピッカー）の手動確認と、PR #30 による自動テスト追加の案内。
