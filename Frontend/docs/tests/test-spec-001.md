# テスト仕様書 001 — クリーンアーキテクチャ リファクタリング

作成日: 2026-05-01  
テストランナー: Bun Test  
対象ブランチ: `feature/refactor/phase7-integration`

> **追記**: テスト件数の増分・デモ重要語・`resetSession` などは `test-spec-002.md` を参照。

---

## 結果サマリー

| 項目 | 結果 |
|------|------|
| 総テスト数 | 42 |
| 合格 | 42 |
| 失敗 | 0 |
| スキップ | 0 |
| 実行時間 | ~70ms |

---

## テストファイル一覧

### 1. domain/entities — Term.test.ts

**目的**: `isTerm()` 型ガード関数の正確性を確認する。

| # | テスト名 | 期待結果 | 結果 |
|---|---------|---------|------|
| 1 | 正常なTermオブジェクトを受け入れる | `isTerm` が `true` を返す | ✅ |
| 2 | externalUrlなしでも有効 | オプショナルフィールド欠如でも通過 | ✅ |
| 3 | nullを拒否する | `isTerm(null)` が `false` を返す | ✅ |
| 4 | プリミティブ値を拒否する | 文字列・数値は拒否 | ✅ |
| 5 | 必須フィールドが欠けているオブジェクトを拒否する | 部分オブジェクトは拒否 | ✅ |
| 6 | relatedTermsが配列でない場合を拒否する | 配列以外は拒否 | ✅ |

---

### 2. domain/entities — Layout.test.ts

**目的**: `isLeafNode()` / `isSplitNode()` 型ガードの正確性を確認する。

| # | テスト名 | 期待結果 | 結果 |
|---|---------|---------|------|
| 1 | LeafNodeを正しく識別する | `isLeafNode` が `true` を返す | ✅ |
| 2 | SplitNodeをLeafNodeとして識別しない | `isLeafNode` が `false` を返す | ✅ |
| 3 | SplitNodeを正しく識別する | `isSplitNode` が `true` を返す | ✅ |
| 4 | LeafNodeをSplitNodeとして識別しない | `isSplitNode` が `false` を返す | ✅ |

---

### 3. infrastructure/importance — FrequencyStrategy.test.ts

**目的**: 頻度ベース重要度計算ストラテジーの動作を確認する。

| # | テスト名 | 期待結果 | 結果 |
|---|---------|---------|------|
| 1 | 頻度とクリック数が高いほどスコアが高い | 高頻度 > 低頻度 | ✅ |
| 2 | 結果は0以上1以下に正規化される | スコアが [0, 1] の範囲内 | ✅ |
| 3 | 頻度0・クリック0のとき最小スコアを返す | スコアが 0 | ✅ |

---

### 4. infrastructure/importance — VectorSimilarityStrategy.test.ts

**目的**: ベクトル類似度ベース重要度計算ストラテジーの動作を確認する。

| # | テスト名 | 期待結果 | 結果 |
|---|---------|---------|------|
| 1 | テーマに近いベクトルのスコアが高い | 類似ベクトル > 非類似ベクトル | ✅ |
| 2 | ベクトルがない場合は頻度のみで計算する | フォールバック動作が正常 | ✅ |
| 3 | 結果は0以上1以下に正規化される | スコアが [0, 1] の範囲内 | ✅ |

---

### 5. infrastructure/storage — LayoutRepository.test.ts

**目的**: localStorage を介したレイアウト永続化の動作を確認する。  
**注意**: テスト環境（Bun）では `localStorage` が存在しないため、ダミー `Storage` を注入して検証する（レイジー初期化で本番実行時のクラッシュを防止）。

| # | テスト名 | 期待結果 | 結果 |
|---|---------|---------|------|
| 1 | 保存したレイアウトを読み込める | `load(phaseId)` が保存値を返す | ✅ |
| 2 | 保存していないフェーズはnullを返す | 未保存フェーズで `null` | ✅ |
| 3 | 上書き保存できる | 2回目の `save()` が値を更新する | ✅ |
| 4 | 現在のレイアウトをシリアライズして返せる | `serialize()` が JSON 文字列を返す | ✅ |

---

### 6. application/bubble — BubbleImportanceUseCase.test.ts

**目的**: バブル生成ユースケースと DI によるストラテジー切り替えを確認する。

| # | テスト名 | 期待結果 | 結果 |
|---|---------|---------|------|
| 1 | Termからバブルを生成できる | `Bubble` エンティティが正しく生成される | ✅ |
| 2 | 複数のTermからバブルリストを生成できる | バブル配列が返される | ✅ |
| 3 | 重要度に基づいてソートできる | `sortByImportance()` が降順に並べる | ✅ |
| 4 | ストラテジーを差し替えられる | `setStrategy()` 後に新ストラテジーで計算される | ✅ |

---

### 7. application/layout — LayoutUseCase.test.ts

**目的**: レイアウト設定・取得と `getLayout()` API の動作を確認する。

| # | テスト名 | 期待結果 | 結果 |
|---|---------|---------|------|
| 1 | レイアウトを設定・取得できる | `setLayout()` → `getLayout()` で同じ値が返る | ✅ |
| 2 | 設定されていないフェーズはnullを返す | 未設定フェーズで `null` | ✅ |
| 3 | getCurrentLayoutはシリアライズされたJSONを返す | `getCurrentLayoutJson()` が JSON 文字列を返す | ✅ |

---

### 8. stores — phaseStore.test.ts

**目的**: フェーズ管理 Zustand ストアの状態遷移を確認する。

| # | テスト名 | 期待結果 | 結果 |
|---|---------|---------|------|
| 1 | 初期フェーズは during | `currentPhaseId === 'during'` | ✅ |
| 2 | フェーズを after に遷移できる | `transitionTo('after')` で `after` になる | ✅ |
| 3 | フェーズを during に戻せる | `transitionTo('during')` で `during` に戻る | ✅ |

---

### 9. stores — termStore.test.ts

**目的**: 用語管理 Zustand ストアのすべての操作を確認する。

| # | テスト名 | 期待結果 | 結果 |
|---|---------|---------|------|
| 1 | 用語を追加できる | `addTerms()` 後に `activeTerms` に含まれる | ✅ |
| 2 | 重複IDの用語は追加されない | 同じ ID を 2 回追加しても 1 件のみ | ✅ |
| 3 | 用語を選択できる | `selectTerm()` 後に `selectedTerm` が更新される | ✅ |
| 4 | ピン留めできる | `togglePin()` 後に `pinnedTermIds` に含まれる | ✅ |
| 5 | ピン留め解除できる | 2 回 `togglePin()` で削除される | ✅ |
| 6 | クリック数を記録できる | `incrementClickWeight()` でカウントが増える | ✅ |
| 7 | 検索履歴に追加できる | `addToHistory()` 後に `searchHistory` に含まれる | ✅ |
| 8 | 履歴は重複を追加しない | 同じ用語を 2 回追加しても 1 件のみ | ✅ |

---

### 10. stores — transcriptStore.test.ts

**目的**: 文字起こし管理 Zustand ストアの動作を確認する。

| # | テスト名 | 期待結果 | 結果 |
|---|---------|---------|------|
| 1 | 初期状態は空のテキストとidleステータス | `transcript === ''`, `status === 'idle'` | ✅ |
| 2 | テキストを設定できる | `setTranscript()` 後にテキストが更新される | ✅ |
| 3 | テキストをクリアできる | `clear()` 後に初期状態に戻る | ✅ |
| 4 | ステータスを設定できる | `setStatus('listening')` で更新される | ✅ |

---

## カバレッジ対象外（ブラウザAPI依存）

以下はブラウザ API に依存するため自動テスト対象外。手動テストで確認する。

| コンポーネント | 理由 |
|-------------|------|
| `WebSpeechTranscriptionService` | Web Speech API（ブラウザ専用） |
| `presentation/` の全コンポーネント | DOM / React レンダリング環境が必要 |
| `app/db/` | IndexedDB（ブラウザ専用）|
| `LayoutEngine` | React / DOM 依存 |

---

## 手動テスト確認項目

`bun run dev` での動作確認時に以下を確認してください。

- [ ] アプリが起動し、発表中画面が表示される
- [ ] マイクボタンを押すと音声認識が開始される
- [ ] 「発表終了」ボタンで発表後画面に遷移する
- [ ] 「発表中に戻る」ボタンで発表中画面に戻る
- [ ] レイアウトプリセットメニューで画面分割が切り替わる
- [ ] バブルクラウドウィンドウが表示される
- [ ] 用語詳細ウィンドウが表示される
